name: Blender Render  # Workflow name

on:
  push:  # ✅ Runs on every commit to any branch
  workflow_dispatch:  # ✅ Allows manual triggering too

jobs:
  render-blender:
    runs-on: ubuntu-latest
    steps:

      # 1️⃣ Step: Checkout the Repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # 2️⃣ Step: Install System Dependencies
      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y python3 python3-pip libegl1 ffmpeg

      # 3️⃣ Step: Install Blender
      - name: Install Blender
        run: |
          wget https://download.blender.org/release/Blender4.4/blender-4.4.3-linux-x64.tar.xz
          tar -xvf blender-4.4.3-linux-x64.tar.xz
          mv blender-4.4.3-linux-x64 ~/blender
          sudo ln -s ~/blender/blender /usr/local/bin/blender

      # 4️⃣ Step: Install Python Dependencies for Dropbox Operations
      - name: Install Python Dependencies
        run: |
          pip install dropbox requests

      # 5️⃣ Step: Remove Previous `simulation_output.blend` File
      - name: Delete Previous Simulation Output
        run: |
          if [ -f testing-input-output/simulation_output.blend ]; then
            rm testing-input-output/simulation_output.blend
            echo "🗑️ Deleted previous simulation_output.blend file."
          else
            echo "⚠️ No previous simulation_output.blend found—proceeding with new simulation."
          fi

      # 6️⃣ Step: Run `fluid_simulation.py` to Create the `.blend` File
      - name: Run Fluid Simulation
        run: |
          python3 fluid_simulation.py

      # 7️⃣ Step: Verify `simulation_output.blend` Creation in `testing-input-output/`
      - name: Verify Simulation Output File
        run: |
          if [ -f testing-input-output/simulation_output.blend ]; then
            echo "✅ Simulation completed—found new simulation_output.blend file."
          else
            echo "❌ ERROR: No new `simulation_output.blend` file detected!"
            exit 1
          fi

      # 8️⃣ Step: Run `main.py` to Download Files and Render Frames
      - name: Run `main.py` for Rendering
        env:
          APP_KEY: ${{ secrets.APP_KEY }}
          APP_SECRET: ${{ secrets.APP_SECRET }}
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
        run: |
          python3 main.py

      # 9️⃣ Step: Create Video from Rendered Frames
      - name: Create Video
        run: |
          chmod +x create_video.sh
          ./create_video.sh

      # 🔟 Step: Upload Final Video to Dropbox
      - name: Upload Video to Dropbox
        env:
          APP_KEY: ${{ secrets.APP_KEY }}
          APP_SECRET: ${{ secrets.APP_SECRET }}
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
        run: |
          python3 upload_to_dropbox.py ./RenderedOutput/video.mp4 /simulations/Blender/output/video.mp4 $REFRESH_TOKEN $APP_KEY $APP_SECRET
