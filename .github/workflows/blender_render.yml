name: Blender Render  # Workflow name

on:
  push:  # ✅ Runs on every commit to any branch
  workflow_dispatch:  # ✅ Allows manual triggering too

jobs:
  render-blender:
    runs-on: ubuntu-latest
    steps:

      # 1️⃣ Step: Checkout the Repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # 2️⃣ Step: Install System Dependencies
      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y python3 python3-pip libegl1 ffmpeg

      # 3️⃣ Step: Install Blender
      - name: Install Blender
        run: |
          wget https://download.blender.org/release/Blender4.4/blender-4.4.3-linux-x64.tar.xz
          tar -xvf blender-4.4.3-linux-x64.tar.xz
          mv blender-4.4.3-linux-x64 ~/blender
          sudo ln -s ~/blender/blender /usr/local/bin/blender

      # 4️⃣ Step: Verify Blender Installation
      - name: Check Blender Installation
        run: |
          ls -l ~/blender
          ~/blender/blender --version || echo "❌ Blender is NOT installed!"

      # 5️⃣ Step: Debug Blender Path
      - name: Debug Blender Path
        run: |
          which blender || echo "❌ Blender path is NOT found!"

      # 6️⃣ Step: Detect Velocity Data File in `testing-input-output/`
      - name: Detect Velocity Data File
        run: |
          if [ -f testing-input-output/velocity_data.json ]; then
            echo "✅ Found `velocity_data.json` in `testing-input-output/`."
          else
            echo "⚠️ WARNING: No `velocity_data.json` found in `testing-input-output/`. Fluid simulation may not run correctly!"
          fi

      # 7️⃣ Step: Search for Any `.blend` File in `testing-input-output/`
      - name: Detect Any `.blend` File
        run: |
          BLEND_FILE=$(ls testing-input-output/*.blend | head -n 1 || echo "")
          if [ -z "$BLEND_FILE" ]; then
            echo "⚠️ WARNING: No `.blend` file found in directory. Simulation will not run!"
          else
            echo "✅ Found `.blend` file: $BLEND_FILE"
            echo "BLEND_FILE=$BLEND_FILE" >> $GITHUB_ENV
          fi

      # 8️⃣ Step: Run Fluid Simulation with Velocity Data
      - name: Run Fluid Simulation
        run: |
          if [ -n "$BLEND_FILE" ] && [ -f testing-input-output/velocity_data.json ]; then
            ~/blender/blender -b --python fluid_simulation.py -- "$BLEND_FILE" --velocity_data testing-input-output/velocity_data.json
          else
            echo "⚠️ SKIPPED: Missing `.blend` or `velocity_data.json`, skipping simulation."
          fi

      # 🔟 Step: Verify `.blend` File Creation in `testing-input-output/`
      - name: Verify `.blend` File Creation
        run: |
          ls -l ./testing-input-output/ || echo "⚠️ WARNING: No `.blend` file was created."

      # 🔟 Step: Commit and Push `.blend` File to Repository (Only If It Exists)
      - name: Commit and Push `.blend` File
        env:
          GIT_USER_NAME: ${{ secrets.GIT_USER_NAME }}
          GIT_USER_EMAIL: ${{ secrets.GIT_USER_EMAIL }}
        run: |
          git config --global user.name "$GIT_USER_NAME"
          git config --global user.email "$GIT_USER_EMAIL"

          if ls testing-input-output/*.blend 1> /dev/null 2>&1; then
            git add testing-input-output/*.blend
            git status
            git diff --cached --quiet && echo "✅ No changes to commit!" || git commit -m "Auto-update: Added latest fluid simulation .blend file"
            git push origin HEAD
          else
            echo "⚠️ SKIPPED: No `.blend` file detected, skipping commit."
          fi
